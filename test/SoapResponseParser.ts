import {} from 'ts-jest';
import ResponseParser from '../src/soap/ResponseParser';

describe('ResponseParser', () => {
    describe('parse', () => {
        it('maps tags to ', async () => {

        });
    });
});